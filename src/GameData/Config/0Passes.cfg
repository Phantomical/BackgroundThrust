// This config does not actually make any changes, but instead declares some
// FOR identifiers that we use to sequence the patches.

// Initial patching, adding modules to the relevant types
@DOES_NOT_EXIST,0:FOR[zBackgroundThrust] { }

